type Person = {
    fname: string;
    lname: string;
    isMarried: boolean;
    children: [],
    getFullName: function () {
        return `${this.fname} ${this.lname}
    
    }
    }
    ]
console.log(people[0].getFullName());